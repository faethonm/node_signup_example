extends layout

block content
  h1= title
  p This is Sequelize greeting you :)

  h2 Creating a new user
  form(action="/users/create", method="post")
    input(type="text", name="username")
    input(type="submit")

  h2 Users

  ul
  each user in users
    li
      = user.username
      | &nbsp;
      | (
      a(href="/users/" + user.id + "/destroy") Destroy
      | )
      ul
        li
          | Creating a new challenge:
          form(action="/users/" + user.id + "/challenges/create", method="post", style="display: inline")
            input(type="text", name="title")
            input(type="submit")
        each challenge in user.Challenges
          li
            = challenge.title
            | &nbsp;
            | (
            a(href="/users/" + user.id + "/challenges/" + challenge.id + "/destroy") Destroy
            | )
